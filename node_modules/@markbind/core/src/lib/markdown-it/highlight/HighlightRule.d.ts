import { HighlightRuleComponent } from './HighlightRuleComponent';
export declare class HighlightRule {
    ruleComponents: HighlightRuleComponent[];
    constructor(ruleComponents: HighlightRuleComponent[]);
    static parseAllRules(allRules: string, lineOffset: number, tokenContent: string): HighlightRule[];
    static splitByChar(allRules: string, splitter: string): string[];
    static parseRule(ruleString: string, lineOffset: number, lines: string[]): HighlightRule | null;
    shouldApplyHighlight(lineNumber: number): boolean;
    applyHighlight(line: string, lineNumber: number): string;
    static _highlightWholeLine(codeStr: string): string;
    static _highlightWholeText(codeStr: string): string;
    static _highlightPartOfText(codeStr: string, bounds: number[][]): string;
    isLineRange(): boolean;
}
//# sourceMappingURL=HighlightRule.d.ts.map